import 'package:flutter/material.dart';

class ChatPage extends StatelessWidget {
  const ChatPage({super.key, required sourchat});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
